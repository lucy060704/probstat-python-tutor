# G3.6 独立评委最终结论

## 结论

2026-08-08，独立评委 Agent 对冻结的 G1–G3 本地工程快照作最终只读复核：**PASS**。

- P0：0
- P1：0
- P2：0
- 阶段决定：允许关闭 G1–G3 本地工程阶段门
- 比赛提交状态：`competition_submission_ready=false`

本结论只适用于 manifest
`9207e8d4c8cc42150230949f5b3002118dcef848a469730c0a60f887478d24ab` 所代表的本地工程；
不代表教师审核、真实学生效果、Windows 11 实机、比赛材料、ADP 适配或最终提交已经完成。

## 独立复核证据

- 独立把审计生成到临时路径，与正式审计 JSON 逐字比较：完全一致；
- 发布允许列表：163 个文件，逐文件大小和 SHA-256 匹配；
- `engineering_gate_passed=true`；
- 重建结果 `source_manifest_sha256` 与正式 manifest 完全相同；
- 临时包只含允许列表文件，blind/holdout 文件数为 0；
- 导入探针确认 Python 代码来自临时包 `src`，不是原工作树；
- 14 个重建命令全部通过，其中公开 pytest 为 658/658；
- `rebuilt_audit_exact_match=true`，重建审计与源审计逐字段完全一致；
- 审计专项测试 7/7 通过；
- Ruff 和 `git diff --check` 通过；
- 重建机器结果中未发现 `/Users/` 本机个人路径；
- 没有运行或读取 blind/holdout 评测数据；评委没有修改项目文件。

主 Agent 的最终原仓库全量回归另为 671/671，通过 OpenAPI、依赖、ruff 与差异检查。原仓库的
历史隔离测试不进入公开包；公开包使用不依赖失效 blind 标签的 658 项套件。

## 审计过程中修复的问题

评委首轮发现发布包排除了 blind 数据，却仍包含直接加载该文件的旧测试，判为 P1/HOLD。主
Agent 随后：

1. 从公开允许列表排除该仓库专用历史测试；
2. 新增会拒绝实际 blind 路径的公开测试入口；
3. 新增仅按允许列表复制、强制临时源码导入的回归；
4. 建立完整临时包重建和公开命令复验；
5. 把审计/重建/评委元数据排除在自校验清单之外，避免自引用；
6. 冻结全部允许列表文件后重新生成审计与重建证据。

评委定向复审曾因说明文档在重建后变化而再次 HOLD。最终严格按“冻结 → 审计 → 重建 → 精确
比较 → 同哈希评委复核”顺序完成，残留 P1 清零。

## 不阻断本地阶段、但阻断比赛提交的项目

- 教师逐单元签字和教材使用许可归档；
- 10–15 名学生知情同意匿名试点；
- Windows 11 实机完整旅程；
- 申报表、比赛版设计说明书和不超过 5 分钟的视频；
- 本地版本冻结后的 ADP 适配；
- 用户确认正式发布和最终提交。

因此本结论不能被表述为“内部评分达到 90 分”“90% 获一等奖”或“比赛已经可以提交”。
