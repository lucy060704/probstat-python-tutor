# G2.2 描述统计单元教师审核表

## 使用说明

本表用于教师真实审核，不是系统自动通过证明。教师填写日期、结论和签字之前，
两张知识卡、6 道题和本单元都保持 `pending_teacher_review`，不得对外宣称
“教师已审核”。

- 审核教师：________________
- 审核日期：________________
- 使用教材版本：________________
- 总体结论：□ 通过　□ 修改后通过　□ 退回

## A. 原创知识卡审核

文件：`data/rag/sources/mean_median.yaml` 和 `data/rag/sources/variance_std.yaml`

| 审核项 | 通过 | 修改后通过 | 退回 | 修改意见 |
| --- | :---: | :---: | :---: | --- |
| 均值、中位数、方差和标准差的定义与单位准确 | □ | □ | □ | |
| 没有把“有异常值”写成“永远必须用中位数” | □ | □ | □ | |
| 总体与样本方差口径、`ddof` 和 Pandas/NumPy 默认值准确 | □ | □ | □ | |
| 标准差比较限定在一致单位、尺度和计算口径 | □ | □ | □ | |
| 没有从描述统计直接推断因果、数据质量或总体结论 | □ | □ | □ | |
| 案例和讲解为团队原创，没有复制教材长段原文 | □ | □ | □ | |

## B. 均值与中位数题组

### 1. `mean_median_concept_01`

核对数值：总和 `71500`，均值 `14300`，中位数 `5500`。

| 审核项 | 通过 | 修改后通过 | 退回 | 修改意见 |
| --- | :---: | :---: | :---: | --- |
| 选择中位数的理由引用了单个极高收入的影响 | □ | □ | □ | |
| 没有将本题结论写成所有场景的普遍规则 | □ | □ | □ | |
| 中文答案“中位数”可由受控别名正确识别 | □ | □ | □ | |

### 2. `mean_median_python_01`

核对数值：`[4, 6, 10, 20]` 的中位数为 `8`。题目要求学生自行写表达式，
不在题干或数据字段中预先给出答案代码。

| 审核项 | 通过 | 修改后通过 | 退回 | 修改意见 |
| --- | :---: | :---: | :---: | --- |
| 数值、偶数个观测的中位数规则和 Pandas API 准确 | □ | □ | □ | |
| 只允许 `df["value"].median()` 或等价属性列写法，不错接其他列 | □ | □ | □ | |
| 系统只做 AST 静态检查，没有执行学生代码 | □ | □ | □ | |

### 3. `mean_median_interpretation_01`

核对数值：总和 `50000`，均值 `10000`，中位数 `6000`。

| 审核项 | 通过 | 修改后通过 | 退回 | 修改意见 |
| --- | :---: | :---: | :---: | --- |
| 结论只描述当前五个观测，不无条件外推 | □ | □ | □ | |
| 没有把均值误解为大多数人的实际收入 | □ | □ | □ | |
| 中文“中位数”、`6000` 和 `6000元` 可正确识别 | □ | □ | □ | |

## C. 方差与标准差题组

### 4. `variance_std_concept_01`

核对数值：A 组波动为 0；B 组平方离差和为 8，总体方差 `8/3`、
样本方差 `4`。两种一致口径下都是 B 组更大。

| 审核项 | 通过 | 修改后通过 | 退回 | 修改意见 |
| --- | :---: | :---: | :---: | --- |
| 题干虽未指定总体/样本口径，但大小结论不受影响 | □ | □ | □ | |
| 完整解释同时说明两种口径，没有暗中切换分母 | □ | □ | □ | |
| 中文“B组”可正确识别 | □ | □ | □ | |

### 5. `variance_std_python_01`

核对数值：`[2, 4, 6]` 均值为 4，平方离差和为 8，样本方差为 4，
样本标准差为 2。

| 审核项 | 通过 | 修改后通过 | 退回 | 修改意见 |
| --- | :---: | :---: | :---: | --- |
| `s.std()` 默认的 `ddof=1` 与结果 2 准确 | □ | □ | □ | |
| NumPy 变体必须显式写 `np.std(s, ddof=1)` | □ | □ | □ | |
| 其他变量、多余参数、错误 `ddof` 或 `var()` 不会误通过 | □ | □ | □ | |

### 6. `variance_std_interpretation_01`

题干已把“稳定”限定为“成绩更集中于各班均值”。

| 审核项 | 通过 | 修改后通过 | 退回 | 修改意见 |
| --- | :---: | :---: | :---: | --- |
| 小标准差表示相对均值更集中的解释准确 | □ | □ | □ | |
| 不把“更集中”扩展为均分更高、教学更好或因果结论 | □ | □ | □ | |
| 中文“A班”可正确识别 | □ | □ | □ | |

## D. 提示、知识图谱与签字

| 审核项 | 通过 | 修改后通过 | 退回 | 修改意见 |
| --- | :---: | :---: | :---: | --- |
| 6 道题的 L1/L2 未直接给出标准答案 | □ | □ | □ | |
| L3 只给关键中间步骤，L4 才给四维完整解释 | □ | □ | □ | |
| 中心、离散、稳健性与情境解释节点和连边合理 | □ | □ | □ | |
| 本单元与两本教材的章节目录映射合理且不过度声称 | □ | □ | □ | |

必须修改项：

1. ______________________________________________________________________
2. ______________________________________________________________________
3. ______________________________________________________________________

- 教师签字：________________
- 修改复核日期：________________
- 复核结论：□ 通过　□ 仍需修改
