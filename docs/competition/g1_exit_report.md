# G1 诊断事实链总验收报告

## 结论

截至 2026-08-07，G1.1—G1.4 的 Codex 可执行范围已经完成：结构化提交、原子状态提交、多证据
确定性判题、AST 静态代码分析、具体误区标签和证据驱动推荐均可离线运行。是否正式放行 G2 由
评委 Agent 根据本报告和当前可运行代码决定；教师签字属于待补的人类证据，不伪造成已完成。
评委 Agent 最终给出 G1 总阶段门 PASS，P0/P1 均为 0，工程上允许进入 G2 开发。

## 功能验收矩阵

| 要求 | 当前证据 | 结果 |
| --- | --- | --- |
| 答案、推理、Python 文本分离 | Pydantic `LearnerSubmission`、中文校验测试 | 通过 |
| 模型/API 失败不污染状态 | 原子提交、冲突重试、回执幂等测试 | 通过 |
| 正确答案不能覆盖错误推理或代码 | 多证据冲突测试与 development | 通过 |
| 不执行学习者代码 | AST-only 实现、危险载荷与文件写入测试、源码扫描 | 通过 |
| 每条具体诊断引用可观察证据 | `EvidenceFinding` 的 source、quote、rule_id | 通过 |
| 错误时提供渐进建议且不返回下一题 | recommendation 集成与离线旅程 | 通过 |
| 正确时更新掌握度并推荐下一题 | service 集成测试与离线旅程 | 通过 |
| 重复提交不重复计分 | SQLite 幂等测试与五次旅程 | 通过 |
| 无 API Key 可运行 | 五次离线旅程、Streamlit 本地健康检查 | 通过 |

## 自动化结果

### 工程检查

- `python -m pytest -q`：219 passed；
- `ruff check .`：通过；
- `git diff --check`：通过；
- `src/` 中学习者代码路径不存在 `eval()`、`exec()` 或 `compile()` 调用；
- 密钥、私钥和硬编码密码模式扫描无命中；
- 未调用真实 OpenAI API，未产生模型费用。

### 非盲评测

| 数据集 | 判题准确率 | 误区标签 exact-match | 建议匹配率 | 一级提示泄露 | API 失败 |
| --- | ---: | ---: | ---: | ---: | ---: |
| development | 100%（32/32） | 90.62%（29/32） | 100%（32/32） | 0%（0/1） | 0%（0/32） |
| v0.1 历史集 | 94.44%（34/36） | 61.11%（22/36） | 94.44%（34/36） | 0%（0/3） | 0%（0/36） |

历史基线不覆盖；指标差异和三项人工标签争议记录在 G1.4 验收报告中。

## 五次离线主路径

命令：

```bash
.venv/bin/python scripts/g1_offline_demo.py
```

结果为 `offline_mode=true`、`requested_runs=5`、`passed_runs=5`、`all_passed=true`。五名隔离的
匿名演示学习者均依次完成：

1. 获取基础题与一级概念提示；
2. 提交带明确误区的错误回答；
3. 系统判错、引用误区证据且隐藏下一题；
4. 修改理由后订正成功，并收到下一题；
5. 重复提交相同订正，报告复用且历史仍只有两次真实尝试。

每次运行都使用临时 SQLite 文件；离线模式不会创建 Agents SDK 会话数据库。

## Streamlit 启动检查

使用项目虚拟环境在本机临时端口启动 `app.py`，服务成功输出本地 URL；
`/_stcore/health` 返回 `ok`。检查后已正常停止进程，没有发布或暴露长期服务。

## 教师抽查包

- 独立填写表：`docs/competition/g1_teacher_review_form.md`；
- 系统对照表：`docs/competition/g1_teacher_review_system_key.md`；
- 覆盖异常值、Pandas 方法调用、方差/标准差、情境解释、置信水平和分数篡改 6 类失败；
- 教师尚未填写，当前不能宣称“教师审核通过”。开发可以继续准备 G2 草稿，但正式比赛材料必须
  在教师确认后补上日期、评分和必须修改项。

## 已知限制和后续约束

1. 现有 blind 已失去正式盲测资格，G2 必须建立未向主 Agent 暴露的新封存集；
2. v0.1 标签 exact-match 为 61.11%，属于旧标签兼容风险，不得隐瞒；
3. 三项 development 旧标签与原文证据不一致，等待教师复核，业务逻辑不按旧标签特判；
4. `len(...) // 常量` 的静态单位置判断尚未限制除数非零，属于非阻塞加固项；
5. 当前只覆盖 4 个知识点和 12 道题，不能在 G2 完成前宣称覆盖 8 个深度单元或两本教材；
6. 本报告没有运行失效 blind 的真实性能评测，也没有配置 ADP。
